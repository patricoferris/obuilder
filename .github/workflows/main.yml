name: Main workflow

on:
  pull_request:
  push:
  schedule:
    # Prime the caches every Monday
    - cron: 0 1 * * MON

jobs:
  linux:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
        ocaml-compiler:
          - 4.13.x

    runs-on: ${{ matrix.os }}

    steps:
      - run: |
          sudo apt-get --allow-releaseinfo-change update && \
          sudo apt-get install -y btrfs-progs zfs-dkms zfsutils-linux

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Use OCaml ${{ matrix.ocaml-compiler }}
        uses: ocaml/setup-ocaml@v2
        with:
          ocaml-compiler: ${{ matrix.ocaml-compiler }}

      - run: opam install . --deps-only --with-test

      - run: ./.run-gha-tests.sh
  macos:
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-latest
        ocaml-compiler:
          - 4.13.x
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Use OCaml ${{ matrix.ocaml-compiler }}
        uses: ocaml/setup-ocaml@v2
        with:
          ocaml-compiler: ${{ matrix.ocaml-compiler }}
      - run: opam install . --deps-only --with-test
      - run: opam exec -- dune runtest
      # macOS runners don't have docker
      # - name: Simple macOS tests
      #   run: |
      #     mkdir var
      #     touch /usr/local/bin/obuilderfs && chmod +x /usr/local/bin/obuilderfs
      #     sudo mkdir /Users/empty && sudo touch /Users/empty/.obuilder_profile.sh
      #     sudo opam exec -- dune exec -- obuilder macos . --store=rsync:$(PWD)/var -f example.macos.spec --uid=705 --fallback=/tmp --scoreboard=/tmp

